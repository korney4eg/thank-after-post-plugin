name: Upload Release Asset when Tag is pushed

on:
  push:
    tags: 
      - '*' 

jobs:
  build:
    name: Upload Release Asset
    runs-on: ubuntu-latest
    steps:
      - name: Check out code into a workflow directory
        uses: actions/checkout@v2

      - name: Getting latest tag on the repo
        id: latest_tag
        uses: actions-ecosystem/action-get-latest-tag@v1
        with:
          semver_only: true

      - name: Zip Chores
        run: |
          sed -i 's/PLUGIN_VERSION/${{ steps.latest_tag.outputs.tag }}/' thank-after-post-plugin.php
          cd .. &&  ls -l && zip -r thank-after-post.zip  thank-after-post-plugin/ -x '*.git*'

      # - name: Create Release
      #   id: create_release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     tag_name: ${{ steps.latest_tag.outputs.tag }}
      #     release_name: Release ${{ steps.latest_tag.outputs.tag }}
      #     draft: false
      #     prerelease: false
      # - run: sed -i 's/PLUGIN_VERSION/${{ steps.latest_tag.outputs.tag }}/' thank-after-post-plugin.php
      # - run: cd .. &&  ls -l && zip -r thank-after-post.zip  thank-after-post-plugin/ -x '*.git*'
      
      # - name: Upload Release Asset
      #   id: upload-release-asset 
      #   uses: actions/upload-release-asset@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
      #     asset_path: ../thank-after-post.zip
      #     asset_name: thank-after-post.zip
      #     asset_content_type: application/octet-stream
    
      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "thank-after-post.zip"
          name: Release ${{ steps.latest_tag.outputs.tag }}
          token: ${{ secrets.GITHUB_TOKEN }}
